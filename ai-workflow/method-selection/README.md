# Method-selection records

One record per analysis, written from the verified-analytics-project template (`assets/templates/method-selection.md`) before any outcome is computed, and approved by the project owner (H2) before the metrics it needs are defined in `docs/metrics.md`.

Records are added in Sprint 2 Step 2.
