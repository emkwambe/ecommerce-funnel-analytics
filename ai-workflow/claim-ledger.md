# Claim–Evidence Ledger — E-commerce Funnel Analytics

One row per claim published anywhere (site, README, deck). The published verb must match the tier. Template: verified-analytics-project v2 (`assets/templates/claim-ledger.md`); the template's separator row lists 12 columns for 13 headers, corrected here.

Figures are not typed in this file (CLAUDE.md rule 1): each claim is shown with the export fields that fill it, and the live page shows the values.

| ID | Claim (as published) | Tier (suggests / shows / establishes) | Question type | Evidence (export fields) | Assumptions | Validation | Reproduction (R1/R2/R3) | Falsification / controls | Correctness level | Remaining uncertainty | Adversarial outcome | Owner sign-off (H9: date, row and page checked) |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| C1 | "In {month}, {x}% of revenue came from purchases with no observed same-session cart event, and {y}% of purchasing sessions contained only such purchases." (home page, section 1, finding a) | shows | descriptive | x: `purchase_paths.json` `rows["Purchase with no observed same-session cart event"].revenue_share`; y: `kpis.json` `month.sessions_with_purchases_only_of_uncarted_products` ÷ `month.sessions_with_purchase`; month: `kpis.json` `month.period_start` | metrics.md §2 (D1 deduplication), §3 (valid sessions), §4 (revenue, primary), §5 (purchase paths), Changes 2026-09-26 (session-level purchase paths). "Observed" means logged in this file. | dbt: `assert_path_revenue_sums_to_total`, `assert_session_purchase_paths_partition`, `assert_mart_revenue_equals_purchases`; full build with all expected tests run (`ai-workflow/evidence/sprint-1/dbt_build_runs.json`) | R2: `funnel.verify` recomputes revenue share by path and `sessions_with_purchases_only_of_uncarted_products` from the Parquet file without dbt (`ai-workflow/evidence/sprint-1/verify.json`) | n/a: descriptive, no mechanism claimed | empirical (descriptive) | One month (October 2019). Cross-session behavior not examined: a purchase with no observed same-session cart event may have been carted in an earlier session or on another device. | kept (checklist: denominators named; no causal, loss, or priority wording; guarded by `test_home_findings_are_descriptive_only`) | Signed off by owner 2026-09-26: home page §1 and /funnel, desktop and mobile. |
| C2 | "{v}% of viewing sessions had an observed cart event; {c}% of sessions with an observed cart event had an observed purchase of a carted product. These rates use different denominators; comparing them describes where observed sessions end, not why." (home page, section 1, finding b) | shows | descriptive | v: `kpis.json` `month.view_to_cart_session_rate`; c: `kpis.json` `month.cart_session_purchase_rate` | metrics.md §3 (valid sessions), §6 (view-to-cart session rate, cart-session purchase rate), Changes 2026-09-26 item 3 (a carted product has a cart event anywhere in the session; no timing condition) | dbt: `assert_month_sessions_equal_int_sessions`, `assert_cart_sessions_partition`; full build with all expected tests run | R2: `funnel.verify` recomputes both rates from the Parquet file without dbt, within 1e-9 (`ai-workflow/evidence/sprint-1/verify.json`) | n/a: descriptive, no mechanism claimed | empirical (descriptive) | One month (October 2019). Cross-session behavior not examined: sessions are as logged, so a cart event or purchase in another session is not linked. The two rates have different denominators and are not a single funnel. | kept (checklist: denominators stated in the claim itself; no causal, loss, or priority wording) | Signed off by owner 2026-09-26: home page §1 and /funnel, desktop and mobile. |
| C3 | "Revenue ({r}) and revenue with repeat purchase events collapsed ({c}) differ by {d}. The difference is exactly the value of {n} purchase events after the first of the same product in a session, in {p} (session, product) pairs. The data cannot tell whether these are extra units or repeated logging: it has no quantity or order column." (/investigations, revenue figures, answer) | shows | descriptive (diagnostic) | r, c, d: `investigation_revenue_gap.json` `totals.revenue`, `.revenue_repeat_collapsed`, `.revenue_difference`; n: `totals.repeat_purchase_events`; p: `totals.pairs` | metrics.md §2 (D1), §3, §4; Changes 2026-09-26 (Sprint 2 investigations) A items 1–2; method record A (H2). Uncertainty register U1 | dbt `assert_revenue_gap_decomposition_reconciles` (the value equals the published difference; the count equals the surplus purchase events); `test_revenue_gap_export_reconciles_and_was_independently_verified`; full build at `e335f67`, 134 of 134 tests | R2: `funnel.verify`, 83 of 83 analysis A checks match (`ai-workflow/evidence/sprint-2/verify.json`), with a different previous-time method (window frame against self-join). Plus a hand-counted synthetic log through both paths | n/a (no mechanism claimed). Consistency: the Sprint 0 raw-basis surplus minus the contract repeat events equals the exact-duplicate purchase rows removed (`test_repeat_purchase_events_reconcile_with_the_sprint0_raw_basis`) | empirical (descriptive) | One month. Units versus logging cannot be established (U1). | kept (checklist: both revenue figures shown together; the claim ceiling is stated in the claim itself; no mechanism wording) | pending (H9, Step 7) |
| C4 | "{s} of the difference comes from repeat purchase events at the same price as the first purchase event of that product in the session." | shows | descriptive | s: `dimensions.price_vs_first_purchase[group_key=same_price].share_of_difference`, with the `different_price` row alongside | Changes A item 3 (the baseline is the pair's earliest purchase price, owner decision A-D1) | groups sum exactly to the difference (dbt test, export test) | R2 as C3 (`A:price_vs_first_purchase:*`) | n/a | empirical (descriptive) | Same price is consistent with both readings (units or logging); it does not distinguish them. | kept (checklist: "consistent with" wording only; no reading chosen) | pending (H9, Step 7) |
| C5 | "{a} of the difference comes from repeat purchase events within 60 seconds of the previous purchase event of that product in the session, {b} within 5 minutes, and {h} within 1 hour. None is in the same second: identical duplicate rows are removed first (D1)." | shows | descriptive | a, b, h: `thresholds[threshold_seconds=60, 300, 3600].share_of_difference`; the same-second zero: `dimensions.time_since_previous_purchase[group_key=same_second].repeat_purchase_events`; D1 removals: `secondary_cases.exact_duplicate_rows` | Changes A items 3–4; thresholds fixed before the data was seen (search log rows 1–10) | `assert_revenue_gap_thresholds_consistent` (monotone, 0 s equals the same-second group, shared total) | R2 (`A:threshold_*`) | n/a. **Bin-based timing wording is dropped** (owner decision H4): the 60 s bin edge splits a large mass, so the 1–59 s / 1 minute or more split is not published as a pattern | empirical (descriptive) | The shape between the thresholds is not published; an exploratory per-second view (search log rows 12–16, unpublished) informs no claim. | kept (checklist: cumulative thresholds only; no interpretation of the timing) | pending (H9, Step 7) |
| C6 | "Pairs with two purchase events account for {q2} of the difference, three for {q3}, and four or more for {q4}. By top-level category, {top} accounts for {t}; the unknown category accounts for {u}." | shows | descriptive | `dimensions.purchase_events_in_pair[].share_of_difference`; `dimensions.category_top[0].group_label`, `.share_of_difference`; unknown: `dimensions.category_top[group_key=unknown].share_of_difference`; concentration beside each group: `.top_10_pair_share` | Changes A item 3; §9 category rules (unknown level; disclosure that codes can merge ids) | groups sum exactly (dbt and export tests) | R2 (`A:purchase_events_in_pair:*`, `A:category_top:*`) | n/a | empirical (descriptive) | A category's share partly reflects its share of revenue; the claim makes no comparison across categories. | kept (checklist: no ranking language beyond order by value; unknown share stated) | pending (H9, Step 7) |
| C7 | "Of the {n1} exact duplicate rows removed (D1), {c} are cart events, {v} views, and {pr} purchase events. The difference between the Sprint 0 count of cart events with no view at or before them ({raw}) and the published count ({pub}) is entirely rows removed as exact duplicates ({dup}); none is in excluded sessions." (/investigations, secondary cases) | shows | descriptive | `secondary_cases.exact_duplicate_rows[]` (by `event_type`, summed `rows_removed`); `secondary_cases.cart_no_view_reconciliation.raw_basis_count`, `.contract_basis_count`, `.removed_as_exact_duplicates`, `.in_null_session_events`, `.in_multi_user_sessions`, `.remainder` (= 0) | Changes A items 5–6; §10 bases | dbt `assert_duplicate_rows_breakdown_reconciles`, `assert_cart_no_view_reconciliation`; `test_revenue_gap_secondary_cases_match_sprint0_and_sprint1_on_their_bases` | R2 (`A-S1:*`, `A-S2:*`), with a first-view method against the dbt `NOT EXISTS` | n/a | empirical (descriptive) | None beyond the data's scope. | kept (checklist: bases named) | pending (H9, Step 7) |

**False-discovery gate for C3–C7 (verified-analytics-project gate 6):**
1. Hypotheses examined: none tested; A is descriptive.
2. Specifications: 10, all pre-specified (search log rows 1–10), plus 5 exploratory diagnostics (rows 12–16) that inform no published claim.
3. Selected after seeing outcomes: no cut was dropped or re-specified. The bin-based timing wording was **removed** after seeing the data, under the method record's pre-stated failure condition (owner decision H4); nothing was added.
4. Evaluation data reused: descriptive, with no holdout.
5. Multiple comparisons: not applicable (no tests).
6. Robustness: the threshold sensitivity (pre-specified); the concentration diagnostic; the Sprint 0 raw-basis consistency check.
7. Mechanism: none claimed; both readings stay open (U1).
8. Practical size: the difference is shown in money beside both revenue figures.

**Tier rules:**
- **establishes:** pre-specified, confirmatory, R2 + R3, controls passed;
- **shows:** confirmatory, R2, assumptions stated;
- **suggests:** exploratory, a single path, or assumptions untested.
