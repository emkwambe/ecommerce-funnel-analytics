# Data profile: October 2019 event log

> Generated by `python -m funnel.profile` from `data/parquet/2019-Oct.parquet`. Do not edit by hand.
> Structural profile only (metric lock, CLAUDE.md rule 3): no rates, revenue, or breakdowns.
> Dataset SHA-256 `fedd938409b5f836ec89b39c861b13dad99fc7cd9beb1fddd97a2d50488b5b80` · git `2eb8862b4fb157a7eb8ba81e47618b3ab26f0fce` · generated 2026-09-26T03:20:01+00:00.

## 1. Schema and completeness

Rows: 42448764.

| Column | Type | Null count | Null share |
|---|---|---|---|
| `event_time` | `TIMESTAMP` | 0 | 0.0 |
| `event_type` | `VARCHAR` | 0 | 0.0 |
| `product_id` | `BIGINT` | 0 | 0.0 |
| `category_id` | `BIGINT` | 0 | 0.0 |
| `category_code` | `VARCHAR` | 13515609 | 0.318398 |
| `brand` | `VARCHAR` | 6113008 | 0.144009 |
| `price` | `DOUBLE` | 0 | 0.0 |
| `user_id` | `BIGINT` | 0 | 0.0 |
| `user_session` | `VARCHAR` | 2 | 0.0 |

Distinct counts (id-like columns): `product_id` 166794, `category_id` 624, `user_id` 3022290, `user_session` 9244421.

## 2. Time

- `event_time` type: `TIMESTAMP`; values displayed in UTC.
- Min: 2019-10-01 00:00:00; max: 2019-10-31 23:59:59.
- Events before 2019-10-01 UTC: 0; on or after 2019-11-01 UTC: 0; null: 0.
- Events with sub-second precision: 0.
- Raw text format (from ingest, before type inference): rows not matching `^\d{4}-\d{2}-\d{2} \d{2}:\d{2}:\d{2} UTC$`: 0; suffixes after `HH:MM:SS`: `' UTC'` 42448764.

## 3. Event types (overall counts only)

| event_type | Events |
|---|---|
| `view` | 40779399 |
| `cart` | 926516 |
| `purchase` | 742849 |

## 4. Duplicates

| Check | Surplus rows | Duplicate groups | Groups with non-identical rows |
|---|---|---|---|
| Exact duplicate rows | 30220 | 18099 | n/a |
| Same (user_session, product_id, event_type, event_time) | 30221 | 18100 | 1 |
| Same (user_session, product_id, event_type), same second | 30221 | 18100 | 1 |

Surplus rows are rows beyond the first in each group. Null keys group together.

## 5. Price

- Zero: 68673 (purchase events: 0); negative: 0 (purchase events: 0); null: 0.
- Quantiles over all events: min 0.0, p01 5.89, p50 162.93, p99 1741.34, max 2574.07.
- Products with more than one distinct price: 57550; maximum distinct prices for one product: 98.

## 6. Categories and brands

| Column | Null | Empty string | Null or empty | Null-or-empty share |
|---|---|---|---|---|
| `category_code` | 13515609 | 0 | 13515609 | 0.318398 |
| `brand` | 6113008 | 0 | 6113008 | 0.144009 |

- `category_id` values mapping to more than one `category_code`: 0.
- `category_id` values with both null and non-null codes: 0.
- `category_code` values mapping to more than one `category_id`: 55.

## 7. Sessions and users

- Events with null `user_session`: 2.
- Non-null sessions: 9244421; with more than one `user_id`: 348; spanning more than 24 hours: 17086.
- Events per session: min 1, p25 1, p50 2, p75 5, p90 10, p99 32, max 1159.

## 8. Ordering anomalies (counts only)

Evaluated within (`user_session`, `product_id`).

- Purchase events evaluated: 742849; with no view of that product in the session: 1276; with no cart of that product in the session: 406639.
- Cart events evaluated: 926514; with no view at or before the cart: 4655; with no view strictly before: 7336.
- Purchase or cart events not evaluable (null session or product): 2.

## 9. Order reconstruction

- Order or transaction ID columns: none found.
- Sessions with at least one purchase event: 629560; with more than one: 80261.
- (`user_session`, `product_id`) pairs with more than one purchase event: 41406 (52477 surplus events); with repeats in the same second: 75.

## Decisions needed before metric definitions

Generated from the counts above. Options are listed neutrally; the project owner decides.

### D1. Deduplication (observed)

- Exact duplicate rows: 30220 surplus rows in 18099 groups.
- Same (user_session, product_id, event_type, event_time): 30221 surplus rows in 18100 groups; 1 of those groups contain rows that differ in another column.
- Same (user_session, product_id, event_type) within one second: 30221 surplus rows in 18100 groups; 1 contain non-identical rows.

**Choice forced:** Which rows count as one event before any funnel count.

**Options:**

1. Keep all rows as logged.
2. Drop exact duplicate rows only.
3. Collapse on (user_session, product_id, event_type, event_time).
4. Collapse on (user_session, product_id, event_type) within the same second.
5. Apply different rules per event type (for example, collapse purchases only).

### D2. Zero, negative, and null prices (observed)

- Zero-price events: 68673 (of which purchase events: 0).
- Negative-price events: 0 (of which purchase events: 0).
- Null-price events: 0.

**Choice forced:** Whether zero, negative, or null priced purchases count toward revenue and toward purchase counts.

**Options:**

1. Count them as purchases and as revenue at their logged price.
2. Count them as purchases but exclude them from revenue.
3. Exclude them from both purchases and revenue.
4. Flag them in a separate data-quality metric only.

### D3. Price varies for the same product (observed)

- Products with more than one distinct price: 57550; maximum distinct prices for one product: 98.

**Choice forced:** Which price is the value of a purchase, or of carted products with no observed purchase in the session.

**Options:**

1. Use the price logged on each event.
2. Use the price on the purchase event for purchases and on the cart event for cart events.
3. Use a per-product reference price (for example, the median across the month).

### D4. Order definition without an order ID (observed)

- Order or transaction ID columns found: none.
- Sessions with at least one purchase event: 629560; with more than one: 80261.
- (user_session, product_id) pairs with more than one purchase event: 41406 (52477 surplus events); pairs with repeats in the same second: 75.
- Publisher statement (evidence only, not a decision), verbatim from the Kaggle dataset page: "A session can have multiple **purchase** events. It's ok, because it's a single order."

**Choice forced:** What one order is, which determines order counts and order value.

**Options:**

1. One order per session that has at least one purchase event.
2. One order per (session, product) purchase pair.
3. One order per purchase event.
4. Purchase events grouped by user_id within a time window.

### D5. Purchases without a view or cart of that product in the session (observed)

- Purchase events evaluated: 742849.
- With no view of the product in the session: 1276.
- With no cart of the product in the session: 406639.

**Choice forced:** Whether the funnel requires the preceding steps in the same session.

**Options:**

1. Count them in the purchase step regardless of earlier steps.
2. Exclude them from step-to-step funnel counts but keep them in totals.
3. Attribute earlier steps across sessions by user_id.
4. Treat purchases without a cart as a separate path (for example, direct buy).

### D6. Cart events with no prior view (observed)

- Cart events evaluated: 926514.
- No view at or before the cart time in the session: 4655.
- No view strictly before the cart time (same-second views excluded): 7336.

**Choice forced:** Whether view-before-cart is required, and whether a same-second view counts as prior.

**Options:**

1. Require no ordering; count any cart.
2. Require a view at or before the cart in the same session.
3. Require a view strictly before the cart.
4. Look for the view across sessions by user_id.

### D7. Missing or inconsistent category and brand (observed)

- category_code null or empty: 13515609 events.
- brand null or empty: 6113008 events.
- category_id values mapping to more than one category_code: 0; with both null and non-null codes: 0.
- category_code values mapping to more than one category_id: 55.

**Choice forced:** How category and brand breakdowns treat missing and inconsistent labels.

**Options:**

1. Group missing labels as an explicit 'unknown' level.
2. Break down by category_id instead of category_code.
3. Exclude missing labels from category or brand breakdowns only.
4. Map each category_id to one code by a stated rule.

### D8. Session anomalies (observed)

- Events with null user_session: 2.
- Sessions with more than one user_id: 348.
- Sessions spanning more than 24 hours: 17086.

**Choice forced:** Whether the session is a valid funnel unit as logged.

**Options:**

1. Use user_session as logged.
2. Exclude null, multi-user, or over-24-hour sessions.
3. Split sessions by user_id, or re-sessionize by an inactivity gap.
4. Use user_id with a time window instead of sessions.

### D9. Event types beyond view, cart, purchase (not observed in this file)

- Event type levels observed: view, cart, purchase.
- Counts per level: view 40779399, cart 926516, purchase 742849.

**Choice forced:** Which event types enter the funnel.

**Options:**

1. Use view, cart, and purchase only; ignore other levels.
2. Use other levels (for example, remove_from_cart) as a separate cart-removal signal.
3. Net cart events against removals before counting cart.

### D10. Time zone and month boundary (observed)

- event_time type: TIMESTAMP; range 2019-10-01 00:00:00 to 2019-10-31 23:59:59 (displayed in UTC).
- Events outside October 2019 in UTC: 0.
- The store's local time zone is not stated in the data.

**Choice forced:** Which clock defines days and hours for time breakdowns, and how boundary events are handled.

**Options:**

1. Use UTC as logged.
2. Convert to a stated store-local time zone.
3. Drop or keep events outside October 2019 in UTC.
